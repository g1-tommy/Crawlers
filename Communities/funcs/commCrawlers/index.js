module.exports = {
    CT01 (timestamp) {
        crawler = require('./CT01')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT02 (timestamp) {
        crawler = require('./CT02')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT03 (timestamp) {
        crawler = require('./CT03')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT04 (timestamp) {
        crawler = require('./CT04')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT05 (timestamp) {
        crawler = require('./CT05')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT06 (timestamp) {
        crawler = require('./CT06')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT07 (timestamp) {
        crawler = require('./CT07')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT08 (timestamp) {
        crawler = require('./CT08')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT09 (timestamp) {
        crawler = require('./CT09')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT10 (timestamp) {
        crawler = require('./CT10')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT11 (timestamp) {
        crawler = require('./CT11')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT12 (timestamp) {
        crawler = require('./CT12')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT13 (timestamp) {
        crawler = require('./CT13')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT14 (timestamp) {
        crawler = require('./CT14')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
    CT15 (timestamp) {
        crawler = require('./CT15')
        crawler.setTimestamp(timestamp)
        return crawler
    } ,
}